// Declaración mínima para el módulo `feather-icons`.
// Se declara como `any` porque la librería no proporciona tipos oficiales.
declare module 'feather-icons' {
  const feather: any;
  export = feather;
}
